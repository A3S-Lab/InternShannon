API_SERVER_ENABLED=true
API_SERVER_HOST=0.0.0.0
API_SERVER_PORT=8642
API_SERVER_KEY=change-me-safeclaw

# SafeClaw talks to Hermes server-to-server, so CORS is usually unnecessary.
# Uncomment only when a browser must call Hermes directly.
# API_SERVER_CORS_ORIGINS=http://127.0.0.1:29653
