// Desktop compatibility export. Use the canonical global filter so cloud and
// desktop modes share the same public API error envelope.
export { GlobalErrorFilter as HttpExceptionFilter } from '../../common/errors';
