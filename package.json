{
  "name": "@shuxiaoan/workspace",
  "version": "0.1.0",
  "private": true,
  "description": "Sanitized shared package workspace",
  "scripts": {
    "build": "pnpm -r --if-present build",
    "test": "pnpm -r --if-present test"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.14",
    "typescript": "^5.1.3"
  }
}
